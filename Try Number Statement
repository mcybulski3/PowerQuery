Power Query

Try Number Statement

Code:

if (try Number.From(Text.Start([Column1],10))otherwise null)
      =null then null else [Column1]



Definition

Allows user to test a number character of a [Column] at specified position and return that line to separate column.
Can be replaced with Text.Middle([Column1],5,10). First number specifies position. 
Second number specifies how many characters to look for.

